font_family HackNerdFont 
background_opacity 0.95
enable_audio_bell no
hide_window_decorations yes
draw_minimal_borders yes

## Animate cursor 
cursor_blink_interval     0
cursor_trail 1
cursor_trail_decay 0.1 0.4

# The cursor shape can be one of (block, beam, underline)
cursor_shape     block

shell fish
editor nvim

# For themes: https://github.com/dexpota/kitty-themes
# include ./themes/solarized_dark_patched.conf
include ./themes/catppuccin.conf

# background #1E1E1E
# echo synIDattr(hlID("Normal"), "bg") in term in vim to see bg color
background #1a212e
font_size 14.0

# The width (in pts) of window borders. Will be rounded to the nearest number of pixels based on screen resolution.
# window_border_width 0

# The window margin (in pts) (blank area outside the border)
# window_margin_width 0

# The window padding (in pts) (blank area between the text and the window border)
# window_padding_width 1

#window_padding_width 1 

allow_remote_control yes
map ctrl+shift+enter no_op

shell sh -c "tmux new-session -A -s main"
